// function calculateDogAge(humanYears) {
//   return `Your doggie is ${humanYears * 7} years old in dog years!`;
// }

// console.log(calculateDogAge(1));

function calculateDogAge(humanAge, puppyAge) {
  return `Your doggie is ${humanAge * 7} years old in dog years or ${puppyAge /
    7} in human years!`;
}

console.log(calculateDogAge(1, 49));

// // The Lifetime Supply Calculator

// function calculateSupply(age, amountPerDay) {
//   const maxAge = 80;
//   const amountPerYear = amountPerDay * 365;
//   const restOfYourLife = maxAge - age;
//   return `You will need ${amountPerYear *
//     restOfYourLife} gr. of your favourite snack to last you until the ripe old age of ${maxAge}`;
// }

// console.log(calculateSupply(56, 50));
// console.log(calculateSupply(36, 23));
// console.log(calculateSupply(78, 56));
